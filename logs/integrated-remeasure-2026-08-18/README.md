Re-measurement of the `prop.integrated` figures, the third set the figure audit
found cited with no surviving log, and the only one of the three that was cited
in a patch header intended for upstream.

The audit found it only after being widened past the write-up to cover patch
headers, and it does not flag it even now: the cited value is a whole number,
which the script's pattern cannot see. That blind spot is documented in the
script.

Configuration taken from the log backing the other half of the claim, not
guessed: qwen2.5-1.5B Q4_K_M, ctx 512, batch and micro-batch 8, 8 chunks,
`-fa off`, f32 compute type. A/B/A within one boot, rebuilding each way:

| arm | cited | measured |
|---|---|---|
| patch applied (`integrated = false`) | 11.2 | 11.1910 |
| upstream behaviour (`integrated = prop.integrated`), run 1 | about 167 | 168.5483 |
| upstream behaviour, run 2 | | 168.1212 |
| patch restored | | 11.1910 |

The claim holds. The corrupted value reproduces within about one percent of the
figure quoted, twice, and unlike the corrupted value in the KQV arm it is stable
across repeats. The correct value matches the cited 11.2 and returns
bit-identically after the patch is restored, which is the A/B/A that makes this
an intervention rather than a comparison.

Generated by `scripts/integrated_flag_remeasure.sh`.
