Re-measurement of the arm that shows why two gates are needed: with the gfx1013
entry removed from llama.cpp's RDNA1 macro, perplexity looks healthy while
generation returns garbage.

The write-up had cited a perplexity of 8.9425 for that arm, and an audit found
the figure was backed by no surviving log, on the board or in this repository.
Rather than leave it cited from memory it was measured again, by reverting that
one line in the working tree, rebuilding the HIP backend, and restoring it
afterwards. Both arms reproduce exactly:

| arm | perplexity | generation |
|---|---|---|
| gfx1013 removed from the RDNA1 macro | 8.9425 | `The???????????????????????` |
| restored (the shipped patch) | 8.9442 | `The capital of France is Paris.` |

The perplexity figure matches the cited one to four decimals and the garbled
string matches the one quoted, so the original observation stands as recorded.

Generated by `scripts/macro_removal_remeasure.sh`.
