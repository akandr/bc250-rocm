Does the zeroed fp16 GEMM follow the call, or the particular rocBLAS build it has
always been measured against?

Seven hypotheses have been eliminated for this defect and every one was tested
against the same library, the gfx1013 rocBLAS built locally. That library had
never been varied, which makes it the largest untested constant left around this
defect.

Two attempts to vary it, both blocked, and both recorded because the blocks are
themselves informative:

**System rocBLAS with `HSA_OVERRIDE_GFX_VERSION=10.1.0`.** The override changes
the device identity for everything, not just rocBLAS, and this llama.cpp is built
with `AMDGPU_TARGETS=gfx1013` only. Its own kernels then fail to load with
`invalid device function` before any GEMM is reached. Inconclusive by
construction.

**System rocBLAS with no override.** Both compute types abort immediately with
`CUBLAS_STATUS_INTERNAL_ERROR`, which is the documented behaviour of that library
on real inference shapes and the reason the native build exists. It cannot
service the workload at all, so it cannot host the comparison.

For reference, the native library in the same session behaves as always: f32
returns 9.0975 twice, bit-identical, and f16 returns 23.4547 and 13.9642.

The comparison needs a llama.cpp carrying both `gfx1010` and `gfx1013` code so
the override has something to run, which is being built separately rather than
over the measured binary. Until then, "the defect is in the fp16 path" and "the
defect is in this rocBLAS build" are not distinguished by anything here.

Generated by `scripts/fp16_library_arms.sh`.
